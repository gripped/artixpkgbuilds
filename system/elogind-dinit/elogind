type            = process
command         = /usr/lib/elogind/elogind
smooth-recovery = true
depends-on      = dbus
depends-on      = local.target
before          = login.target
before          = logind
