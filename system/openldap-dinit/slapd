type            = process
command         = /usr/bin/slapd -u ldap -g ldap -d 0 2>&1
smooth-recovery = true
logfile         = /var/log/dinit/slapd.log
depends-on      = net-lo
depends-on      = slapd-pre
