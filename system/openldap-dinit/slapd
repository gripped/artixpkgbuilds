type            = process
command         = /usr/bin/slapd -u ldap -g ldap -h $SLAPD_URLS $SLAPD_OPTIONS
smooth-recovery = true
env-file        = /etc/dinit.d/config/slapd.conf
logfile         = /var/log/dinit/slapd.log
depends-on      = slapd-pre
