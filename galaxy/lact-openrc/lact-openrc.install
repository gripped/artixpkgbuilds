post_upgrade() {
local symlink runlevel

for symlink in /etc/runlevels/*/lactd; do
    [ -L "$symlink" ] || continue

    runlevel=${symlink%/*}
    runlevel=${runlevel##*/}

    rc-update del lactd "$runlevel"
    rc-update add lact "$runlevel"
done

}
