diff --git a/qtbase/cmake/FindWrapZSTD.cmake b/qtbase/cmake/FindWrapZSTD.cmake
index 40476d6..66d4e0d 100644
--- a/qtbase/cmake/FindWrapZSTD.cmake
+++ b/qtbase/cmake/FindWrapZSTD.cmake
@@ -25,10 +25,12 @@ find_package(zstd CONFIG QUIET)
 
 include(FindPackageHandleStandardArgs)
 
-if(TARGET zstd::libzstd_static OR TARGET zstd::libzstd_shared)
+if(TARGET zstd::libzstd OR TARGET zstd::libzstd_static OR TARGET zstd::libzstd_shared)
     find_package_handle_standard_args(WrapZSTD
                                       REQUIRED_VARS zstd_VERSION VERSION_VAR zstd_VERSION)
-    if(TARGET zstd::libzstd_shared)
+    if(TARGET zstd::libzstd)
+        set(zstdtargetsuffix "")
+    elseif(TARGET zstd::libzstd_shared)
         set(zstdtargetsuffix "_shared")
     else()
         set(zstdtargetsuffix "_static")
diff --git a/qttools/cmake/FindWrapLibClang.cmake b/qttools/cmake/FindWrapLibClang.cmake
index c95411b..59013fa 100644
--- a/qttools/cmake/FindWrapLibClang.cmake
+++ b/qttools/cmake/FindWrapLibClang.cmake
@@ -20,6 +20,12 @@ find_package(Clang CONFIG)
 if(TARGET zstd::libzstd_shared)
     qt_internal_disable_find_package_global_promotion(zstd::libzstd_shared)
 endif()
+if(TARGET zstd::libzstd_static)
+    qt_internal_disable_find_package_global_promotion(zstd::libzstd_static)
+endif()
+if(TARGET zstd::libzstd)
+    qt_internal_disable_find_package_global_promotion(zstd::libzstd)
+endif()
 
 if(__qt_wrap_clang_backup_prefix)
     set(CMAKE_PREFIX_PATH "${__qt_wrap_clang_backup_prefix}")
