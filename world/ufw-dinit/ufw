type         = scripted
command      = /usr/bin/ufw enable
stop-command = /usr/bin/ufw disable
restart      = false
logfile      = /var/log/dinit/ufw.log
depends-on   = network-pre
before       = network
