type            = process
command         = /usr/bin/privoxy --no-daemon /etc/privoxy/config
smooth-recovery = true
logfile         = /var/log/dinit/privoxy.log
depends-on      = network.target
before          = login.target
