["dotnet-core"]
source = "github"
github = "dotnet/dotnet"
use_max_release = true
use_release_name = true
include_regex = '^\.NET \d+\.0\.\d+ / \d+\.0\.1\d+\s*$'
from_pattern = '^\.NET (\d+\.0\.\d+) / \d+\.0\.(\d+)\s*$'
to_pattern = '\1.sdk\2'
