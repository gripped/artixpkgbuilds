--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,6 +1,6 @@
 [build-system]
 build-backend = 'setuptools.build_meta'
-requires = ['setuptools>=78', "swig>4.1.0"]
+requires = ['setuptools>=78']
 
 [project]
 name = "pygraphviz"
