type            = process
command         = /usr/bin/radicale -C /etc/radicale/config
smooth-recovery = true
logfile         = /var/log/dinit/radicale.log
depends-on      = network.target
before          = login.target
